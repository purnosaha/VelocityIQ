FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir duckdb

COPY scripts/init_db.py ./scripts/init_db.py
COPY sql/init.sql ./sql/init.sql

RUN mkdir -p /data

CMD ["python", "scripts/init_db.py", "--force"]
